--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -132,6 +132,8 @@ cc_library(
         ":tvos_x86_64": COMMON_SRCS + X86_SRCS + MACH_SRCS + MACH_X86_SRCS,
         ":tvos_arm64": COMMON_SRCS + MACH_SRCS + MACH_ARM_SRCS,
         ":emscripten_wasm": COMMON_SRCS + EMSCRIPTEN_SRCS,
+        ":windows_arm64": COMMON_SRCS + ["src/arm/windows/init.c", "src/arm/windows/init-by-logical-sys-info.c"],
+        "//conditions:default": COMMON_SRCS + X86_SRCS + MACH_SRCS + MACH_X86_SRCS,
     }),
     copts = select({
         ":windows_x86_64": [],
@@ -253,6 +255,11 @@ config_setting(
     values = {"cpu": "x64_windows"},
 )

+config_setting(
+    name = "windows_arm64",
+    values = {"cpu": "arm64_windows"},
+)
+
 config_setting(
     name = "android_armv7",
     values = {
